import { SearchProviderNameFilterPipe } from './search-provider-name-filter.pipe';

describe('SearchProviderNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProviderNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
