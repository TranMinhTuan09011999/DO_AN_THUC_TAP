import { SearchProviderFilterPipe } from './search-provider-filter.pipe';

describe('SearchProviderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProviderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
