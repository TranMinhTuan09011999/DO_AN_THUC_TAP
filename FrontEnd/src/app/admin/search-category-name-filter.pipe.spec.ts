import { SearchCategoryNameFilterPipe } from './search-category-name-filter.pipe';

describe('SearchCategoryNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCategoryNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
