import { SearchEmployeeNameFilterPipe } from './search-employee-name-filter.pipe';

describe('SearchEmployeeNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployeeNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
