import { SearchCategoryIdFilterPipe } from './search-category-id-filter.pipe';

describe('SearchCategoryIdFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCategoryIdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
