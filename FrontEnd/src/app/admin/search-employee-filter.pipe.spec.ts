import { SearchEmployeeFilterPipe } from './search-employee-filter.pipe';

describe('SearchEmployeeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployeeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
