import { WarehouseReceiptDetailRequest } from './warehouse-receipt-detail-request';

describe('WarehouseReceiptDetailRequest', () => {
  it('should create an instance', () => {
    expect(new WarehouseReceiptDetailRequest()).toBeTruthy();
  });
});
