import { AccountCustomerUpdate } from './account-customer-update';

describe('AccountCustomerUpdate', () => {
  it('should create an instance', () => {
    expect(new AccountCustomerUpdate()).toBeTruthy();
  });
});
