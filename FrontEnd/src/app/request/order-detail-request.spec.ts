import { OrderDetailRequest } from './order-detail-request';

describe('OrderDetailRequest', () => {
  it('should create an instance', () => {
    expect(new OrderDetailRequest()).toBeTruthy();
  });
});
