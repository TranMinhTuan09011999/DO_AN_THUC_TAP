import { WarehouseReceiptResponse } from './warehouse-receipt-response';

describe('WarehouseReceiptResponse', () => {
  it('should create an instance', () => {
    expect(new WarehouseReceiptResponse()).toBeTruthy();
  });
});
