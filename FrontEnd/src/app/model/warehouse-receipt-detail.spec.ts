import { WarehouseReceiptDetail } from './warehouse-receipt-detail';

describe('WarehouseReceiptDetail', () => {
  it('should create an instance', () => {
    expect(new WarehouseReceiptDetail()).toBeTruthy();
  });
});
