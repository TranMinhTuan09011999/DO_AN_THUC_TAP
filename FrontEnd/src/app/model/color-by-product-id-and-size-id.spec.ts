import { ColorByProductIdAndSizeId } from './color-by-product-id-and-size-id';

describe('ColorByProductIdAndSizeId', () => {
  it('should create an instance', () => {
    expect(new ColorByProductIdAndSizeId()).toBeTruthy();
  });
});
